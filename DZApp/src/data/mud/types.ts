export enum LogisticActionType {
  INJECT = 0,
  TRANSFER = 1,
  EXTRACT = 2,
}

export enum LogisticFixtureType {
  FAUCET = 0,
  SINK = 1,
}

export enum LogisticTransactionType {
  WITHDRAWAL = 0,
  DEPOSIT = 1,
}