/**
 * @file Exports the WalletProvider component and the useConnection hook for easy access.
 */

/**
 * @summary Re-exports the WalletProvider component for easy access.
 */
export { WalletProvider } from "./WalletProvider";
/**
 * @summary Re-exports the useConnection hook from WalletContext for easier access.
 */
export { useConnection } from "./WalletContext";
