/**
 * @file
 * @summary Re-exports the core MUD setup function.
 * @description This file serves as an entry point for the MUD setup module,
 * re-exporting the `setup` function from `./setup`.
 *
 * @exports setup - The main function to set up the MUD client.
 */
import { setup } from "./setup";

/**
 * @summary Re-exports the main MUD setup function.
 */
export { setup };
